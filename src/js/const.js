const pomodoroFormWindow = document.querySelector(".pomodoro-form window");

const pomodoroTasksList = document.querySelector(
  ".pomodoro-tasks__quest-tasks"
);
const addBtn = document.querySelector(".task-form__add-button");

export { pomodoroFormWindow, pomodoroTasksList, addBtn };
